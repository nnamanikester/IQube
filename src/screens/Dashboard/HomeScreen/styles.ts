import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  badge: {
    borderRadius: 10,
    width: 10,
    height: 10,
  },
  badgeContainer: {
    borderRadius: 15,
    position: 'absolute',
    right: 0,
    width: 13,
    height: 13,
  }
});
